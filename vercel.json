{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "VITE_API_URL": {
      "description": "Backend API URL for production (e.g., https://your-backend.railway.app)",
      "required": true
    }
  }
}
